type Props = {}

export default function page({ }: Props) {

// check whether the user is logged in, if not redirect to the login page

// if the user is logged in, check whether the user is the owner of the gym, if not throw an error

// if the user is the owner of the gym, redirect to the dashboard

    return (
        <>
        </>
    )
}